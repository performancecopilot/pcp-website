*** pcp.io website build/configuration ***

1) Install the following packages (Fedora):
dnf install -y git ruby rubygem-haml lynx rsync linkchecker rubygems

2) Install the following rubygem:
  gem install sass-globbing

3) if doing a new PCP release, see NEWRELEASE file.
